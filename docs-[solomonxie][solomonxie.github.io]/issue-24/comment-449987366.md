# Python yield


## 递归+yield

```py
class MyClass:
    def _private(self, num):
        yield num
        if num < 100:
            yield from self._private(num+1)

    def iterate(self, num):
         return self._private(num)

obj = MyClass()
for  i in obj.iterate(100):
    print( i )
```